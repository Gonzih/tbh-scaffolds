%h1 Editing <%= singular_table_name %>

= render 'form'

= link_to 'Destroy', @<%= singular_table_name %>, :confirm => 'Are you sure?', :method => :delete, :class => 'btn danger float_left'
= link_to 'Back', <%= index_helper %>_path, :class => 'btn float_left'
